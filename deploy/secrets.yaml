# TEMPLATE Secrets (TLS bundle, ACL users) — PLACEHOLDERS ONLY, NEVER real
# values (hard security rule, docs/architecture/07-security.md). Populated in
# M1/M5. Provide your own values out-of-band; do not commit real credentials.
---
# apiVersion: v1
# kind: Secret
# metadata:
#   name: cluster1-secrets
# type: Opaque
# stringData:
#   # password placeholders only — supply real values via your secret manager
#   _operator: <REPLACE_ME>
#   _exporter: <REPLACE_ME>
#   _backup: <REPLACE_ME>
