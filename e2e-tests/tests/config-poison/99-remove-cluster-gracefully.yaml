# tests/config-poison/99-remove-cluster-gracefully.yaml  (TestStep)
apiVersion: kuttl.dev/v1beta1
kind: TestStep
timeout: 180
commands:
  - script: |-
      set -o xtrace
      source ../../functions
      kube delete configmap e2e-config-poison-baseline --ignore-not-found
      kube delete pvk cluster1 --wait=true --timeout=150s
