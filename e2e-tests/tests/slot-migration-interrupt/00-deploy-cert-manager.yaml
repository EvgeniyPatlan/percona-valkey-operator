# tests/slot-migration-interrupt/00-deploy-cert-manager.yaml  (TestStep)
apiVersion: kuttl.dev/v1beta1
kind: TestStep
timeout: 300
commands:
  - script: |-
      set -o xtrace
      source ../../functions
      deploy_cert_manager
